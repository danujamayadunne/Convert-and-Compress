import AudioCompressor from "./component"

export const metadata = {
    title: "Audio Compressor",
    description: "Easily compress audio files like MP3, WAV, AAC, FLAC, and OGG with our free online audio compressor. Fast, high-quality, and perfect for any device.",
    keywords: "audio compressor, compress audio online, mp3 compressor, wav compressor, aac compressor, flac compressor, ogg compressor, free audio compressor, online audio compressor, audio format compressor, music compressor, compress audio files, compress audio to mp3, compress audio to wav, compress audio to aac, audio compressor for mobile, transform audio format, high-quality audio compression, fast audio compressor",
};

export default function Page() {
    return <AudioCompressor />
}